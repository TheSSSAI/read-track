[{'name': 'Request-Reply', 'type': 'RequestReply', 'implementation': 'Synchronous communication via HTTP/REST. The Flutter client uses the Dio library to call the ASP.NET Core backend API. The backend uses HttpClient to call external APIs like OpenAI.', 'applicability': 'The primary pattern for all immediate user interactions, such as authentication, fetching data for display (e.g., goals, dashboard), and submitting forms. It is essential for operations requiring real-time feedback.'}, {'name': 'Job Queue', 'type': 'AsynchronousProcessing', 'implementation': 'The ASP.NET Core backend uses the Hangfire library to enqueue long-running or deferrable tasks. These jobs are persisted and executed by background workers, decoupled from the initial HTTP request-response cycle.', 'applicability': 'Essential for non-blocking operations. Specifically required for the user-initiated data export (REQ-FUNC-009) and for running scheduled tasks like the `SubscriptionStatusService` to revert user tiers (REQ-FUNC-002).'}, {'name': 'Client-Side Data Synchronization', 'type': 'OfflineSupport', 'implementation': "The Flutter client's Repository layer serves data from the local Isar database when offline. A `SynchronizationService` is responsible for pushing queued local changes to the backend and pulling remote updates when the device is online.", 'applicability': 'Directly fulfills REQ-FUNC-011, enabling core application functions to work without a network connection. This pattern provides a reliable and responsive user experience despite intermittent connectivity.'}, {'name': 'Retry', 'type': 'FaultTolerance', 'implementation': "Implemented in the backend's Infrastructure layer using the Polly library on HttpClient instances that communicate with external APIs like OpenAI. Configured with policies like exponential backoff.", 'applicability': 'Handles transient faults when communicating with external services as required by REQ-REL-002. Prevents temporary network glitches or brief service unavailability from causing a hard failure for the user.'}, {'name': 'Circuit Breaker', 'type': 'FaultTolerance', 'implementation': "Implemented using the Polly library in the backend's HTTP clients. The breaker trips after a configured number of consecutive failures, preventing further calls to a failing external service for a cool-down period.", 'applicability': 'Protects the system from cascading failures caused by an unresponsive external API (e.g., OpenAI), as mandated by REQ-REL-002. This allows the system to fail fast and maintain stability.'}, {'name': 'Cache-Aside', 'type': 'Performance', 'implementation': "The backend's Application layer logic first attempts to retrieve data from a distributed Redis cache. On a cache miss, data is fetched from the PostgreSQL database and then populated into the cache for subsequent requests.", 'applicability': 'Crucial for meeting the performance requirements of REQ-PERF-001 and REQ-PERF-002. It reduces database load and lowers latency for frequently read data needed for the main Dashboard screen.'}, {'name': 'Rate Limiting', 'type': 'Throttling', 'implementation': "Implemented within the backend's `RecommendationGenerationService` before it calls the external OpenAI API. This is managed using a token bucket or fixed window counter algorithm.", 'applicability': 'A mandatory control to manage costs and adhere to API usage policies, as specified in the constraints for REQ-FUNC-007. It prevents excessive spending on the LLM API.'}]

