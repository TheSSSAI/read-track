This Entity-Relationship diagram illustrates the complete schema for the application's primary database. The central entity is `User`, which connects to most other tables like `LibraryItem`, `Subscription`, `Goal`, and `ReadingSession`. The diagram highlights the one-to-many relationships stemming from the `User` table, representing user-specific data. It also shows how master data, such as the `Book` table, links to user library items. Key entities for user activity tracking, like `ReadingSession` and `TaskCompletion`, are clearly depicted with their respective relationships.