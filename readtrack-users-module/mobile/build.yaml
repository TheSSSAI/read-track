targets:
  $default:
    builders:
      json_serializable:
        options:
          explicit_to_json: true
          check_for_null: true
      riverpod_generator:
        options:
          provider_name_suffix: "Provider"